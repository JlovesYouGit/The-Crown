@echo off
cls
color 0A
title Hardware Target Selection Terminal

echo ========================================================
echo    HARDWARE TARGET SELECTION TERMINAL
echo    CPU/GPU Configuration with Windows DLL Setup
echo ========================================================
echo.
echo Initializing hardware target selection terminal...
echo.
timeout /t 1 /nobreak >nul
echo [SYSTEM] Loading hardware acceleration modules...
timeout /t 1 /nobreak >nul
echo [SYSTEM] Initializing Windows DLL configuration...
timeout /t 1 /nobreak >nul
echo [SYSTEM] Establishing hardware target interface...
timeout /t 1 /nobreak >nul
echo.
echo ========================================================
echo    HARDWARE TERMINAL READY
echo ========================================================
echo.
echo Features:
echo   - CPU/GPU hardware detection
echo   - Windows DLL optimization
echo   - Hardware target selection
echo   - Acceleration benchmarking
echo.
echo Starting terminal interface...
echo.
timeout /t 2 /nobreak >nul

python Hardware_Target_Terminal.py